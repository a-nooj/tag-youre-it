# Security

Tag, You're It! is a fully client-side, static web app — there is no backend, no server-side processing, and no user data is collected or transmitted anywhere.

If you find a security concern anyway (e.g. a supply-chain issue in a dependency, or an XSS-style bug in how marker input is rendered), please open a GitHub issue describing it.
